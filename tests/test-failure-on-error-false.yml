name: Test failure_on_error false with invalid credentials
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        continue-on-error: true
        with:
          dbt_cloud_token: "fake-dbt-token"
          dbt_cloud_account_id: "12345"
          dbt_cloud_job_id: "67890"
          failure_on_error: "false"
          cause: "Test failure_on_error=false run"
          interval: "5"
          wait_for_job: "true"
          get_artifacts: "false"
          fetch_catalog: "false"
      - name: Assert action ran (failure_on_error=false mode)
        env:
          OUTCOME: ${{ steps.action.outcome }}
          RUN_ID: ${{ steps.action.outputs.run_id }}
        run: |
          echo "outcome: $OUTCOME"
          echo "run_id: $RUN_ID"
          if [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action failed gracefully as expected with invalid credentials"
          elif [ "$OUTCOME" = "success" ]; then
            if [ -z "$RUN_ID" ]; then
              echo "FAIL: action succeeded but run_id output is empty"
              exit 1
            fi
            echo "PASS: action succeeded with run_id=$RUN_ID"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
